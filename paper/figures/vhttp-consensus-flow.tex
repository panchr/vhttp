\begin{figure*}
  \centering
  \begin{adjustbox}{width=\textwidth}
  \begin{tikzpicture}[node distance = 2cm, auto]
    \tikzset{every node}=[font=\tiny\sffamily]

    \node [block] at (0, 0) (client) {Client};
    \node [block] at (2, 0) (vhttp) {vHTTP};
    \node [block] at (4, 4) (vp-A) {Vantage Point A};
    \node [block] at (4, 2) (vp-B) {Vantage Point B};
    \node [block] at (4, 0) (vp-C) {Vantage Point C};
    \node [block] at (4, -2) (vp-D) {Vantage Point D};
    \node [block] at (4, -4) (vp-E) {Vantage Point E};
    \node [block] at (6, 0) (vhttp-rec) {vHTTP};
    \node [block] at (8, 0) (client-rec) {Client};

    % Step 1: client->vhttp
    \draw (client) -> (vhttp) node [midway, fill=white, anchor=center, sloped] {Request};

    % Step 2: vhttp->proxies
    \draw (vhttp) -> (vp-A) node [midway, fill=white, anchor=center, sloped] {Proxy Request};
    \draw (vhttp) -> (vp-B) node [midway, fill=white, anchor=center, sloped] {Proxy Request};
    \draw (vhttp) -> (vp-C) node [midway, fill=white, anchor=center, sloped] {Proxy Request};
    \draw (vhttp) -> (vp-D) node [midway, fill=white, anchor=center, sloped] {Proxy Request};
    \draw (vhttp) -> (vp-E) node [midway, fill=white, anchor=center, sloped] {Proxy Request};

    % Step 3: proxies->vhttp
    \draw (vp-A) -> (vhttp-rec) node [midway, fill=white, anchor=center, sloped] {Proxy Response};
    \draw (vp-B) -> (vhttp-rec) node [midway, fill=white, anchor=center, sloped] {Proxy Response};
    \draw (vp-C) -> (vhttp-rec) node [midway, fill=white, anchor=center, sloped] {Proxy Response};
    \draw (vp-D) -> (vhttp-rec) node [midway, fill=white, anchor=center, sloped] {Proxy Response};
    \draw (vp-E) -> (vhttp-rec) node [midway, fill=white, anchor=center, sloped] {Proxy Response};

    % Step 4: vhttp->client
     \draw (vhttp-rec) -> (client-rec) node [midway, fill=white, anchor=center, sloped] {Response};

  \end{tikzpicture}
  \end{adjustbox}
  \caption{Flow of data when vHTTP achieves consensus.}
  \label{fig:vhttp-consensus-flow}
\end{figure*}